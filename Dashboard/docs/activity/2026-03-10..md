# Code Edits (XXhrs)


## Metadata edit page fix - 5

Ensure all cells have the same height and style (remove boarder from availible phases cell) Ensure uniformity in the table strucutre use expert ui ux judgement. Change Gross Vehicle Weight (lbs) to GVW (lbs). Createa 3 individual cells for GVW (lbs), FGAWR (lbs), RGAWR (lbs)

Completed: unified metadata cell styling/height in Edit Metadata, removed the special border style from Applicable Phases, renamed Gross Vehicle Weight to GVW (lbs), and replaced the grouped weight section with individual GVW/FGAWR/RGAWR cells.

## Metadata edit page fix - 6
Issue: `Last updated by` displayed a user UUID instead of a username.
Observed: `Last updated by: fdac1720-6935-4435-a8e5-5cb434657108`
Expected: show a readable username (fallback to UUID or `N/A` if unavailable).


## Import/ Export database 
Evaluate the export database functionality in the database route. Modify this so that when the user clicks export database a popup is displayed to the user to specifiy the path to save the database. Also currently i see a spinner active when i click export database (I think its not wired to actually export a .db file 

SIMILARLY ensure that the import database is wired as well. When the user imports a database (again like with export, display a popup to the user to allow for selection of .db path). NOTE that when user imports a database that database would replace the active one 