# Code Edits (6hrs)


## Metadata edit page fix - 1

- The Edit Metadata page shows new fields that were added to characterize program-versions of events. Currenlty, the database page does not show these new events (also ensure  these new metadata fields are accounted for in the database schema).

- Curenlty the new metadata fields are visible, but ghosted. Users (maybe admin not sure- check this) are not able to edit program-version metadata via the Edit Metadata page. NOTE that for a normal user ONLY the Status field should be ghosted (For Admine not fields are ghosted) NOTE that both user and admin should be able to edit and save. Saving will modify the values for the that program-version in the database AND then those changes will be reflected in the database page column values. ALSO currently there are new columns (new prgram version metadata columns) that are not visible in the database page. I need these meta data fields to be visibile just like the existing metadata fields AND ofcourse ensure that the users have the ability to shown/hide columns in the database page when they change the column visibility in the database page.

- ALSO ensure that after adding the new metadata the event curves can be filtered in the dashboard from within the Global Filter section  

- NEW: Also implement functionality to record changes made to the dataase. Currenlty when the user selects a program and version from the edit metadata page, the following popus up in the right hand side of the page: 

    Selection Metadata
    User ID: N/A
    Last update time: 2/26/2026, 11:57:58 AM
    Uploaded time: 2/26/2026, 11:57:58 AM

Modiy this display to show the following metadata:  

    Selection Metadata
    Last update by: N/A
    Last update time: 2/26/2026, 11:57:58 AM
    Uploaded by: Admin/userid
    Uploaded time: 2/26/2026, 11:57:58 AM
    Status: 


### Critical Clarifications To Lock First
- Single source of truth for “program-version metadata fields" from server/schema.yaml + docs/database-schema.txt so fields are not added in multiple places.
- RBAC behavior: uses can edit all non-Status fields 
- Edits are per program-version globally (so think about how to best handle concurrent user interaction - we can assume no 2 users will be editing the same program-version at the same time - this is unlikely, since only a small group of users will be using the application at any single point of time - IN ANYCASE think of scerios and code complexity to implement so i can choose option)
- Newly added metadata fields must be auto-discoverable in Database page and Global Filters (config-driven) since these are intended to be defined in the database single source of truth. NOTE THIS IS NOT REFEREING TO THE CUSTOM FIELDS (THIS IS LEFT AS A PLACEHOLDER, WE ARE NOT IMPLMENT THIS RIGHT NOW) 
- persistence contract: after edit/save, should UI update via optimistic cache update or refetch from server (for this consider best ui/ux feel, lean implmenetaiton)


## Metadata edit page fix - 2
Currenlty when admin changes the program-version metadata field values and saves, there is no indication of whether the save operation was successfull. Also consider the time it takes for the db update to take place, currenltly if the user updates the fields and switches routes to the database route, the database table will be blank with a spinner animation showing activity - NOTE that i noticed it timeout since the db update took longer - brainstorm lean simple ways to improve ux


## Metadata edit page fix - 3

Refactor the Edit Metadata page to match the Database page visual structure: a full-height split-pane workspace with a collapsible left control panel and a dominant right content panel. Keep the existing Edit Metadata functionality, but change layout/styling to the same design language as Database: compact controls, muted section cards, subtle borders/shadows, toolbar-style top strip, and a scrollable main content area instead of a centered standalone card. Reuse existing shared UI patterns/components from the Database page where possible.NOTE follow SOLID priniciapls and write lean clean bloat free code. 

Modify the Edit Metadata page to have the two-pane composition as in the Database page:
    Left pane: a collapsible utility side panel (about 320px expanded, narrow collapsed rail): This panel should have The header Edit Metadata (match the font size and typograph as in the database page left panel). Then below that have the Prgram ID and Version drop downs. Below that show the Selection Metadata information content (use a better header title for this)

    Right pane: a dominant data surface showing the 2 tabs Filter Values and Custom fields. FOR now NOTE that the custom fields tab should be blank (remove existing content in that custom field tab) INSTEAD: when the user goes to that tab show the following GIF from link: https://www.qqpr.com/ascii/img/ascii-1045.gif (download this locally into appropraite proj directory) Below the gif state message: "Under Construction". For displaying the Filter Values Content Ensure the 2 column style that is currenlty used is maintained. 
    
ALSO change the route name from     /database/filter-values to /database/edit



## Metadata edit page fix - 4

In the Edit Metadata field, for the following fields, the user should not select a weight range from a drop down. Instead they should be able to enter the actual value (NOTE that even though the user enters the actual value, IN THE dashboard Global Filters page, the weight range filters will be used to filter events with weight values that fall between the selected weight range):

    - Gross Vehicle Weight Range (lbs)
    - RGAWR Range (lbs)
    - FGAWR Range (lbs)

Also Phase field variable needs to be replaced. Instead of a single Phase field variable create 4 more: RFQ, DV, PV, Post-Prod.  (update the entire codebase where relavant to update this and remove Phase field): Each of these 4 new fields should have a boolean checkbox with label "Applicable".    In the Edit Metadata right hand area. remove the Phase field. THen to the right of the Status cell in the 2nd column have all 4 new field varibales (so this means that the cell to the right of the Status cell needs to be subdivided into 4 where the RFQ, DV, PV, Post Prod. can be entered with the check boxes below the label but still inside that cell.)

